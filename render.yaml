services: 
  - type: web
    name: BuyIt_Backend_Api
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --no-input
      python manage.py migrate
    startCommand: "gunicorn Backend.wsgi:application"
