services: 
  - type: web
    name: Django_Ecommerce_Backend
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --no-input
      python manage.py migrate
    startCommand: "gunicorn Backend.wsgi:application"
